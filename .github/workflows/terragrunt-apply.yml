name: Terragrunt Apply

on:
  workflow_dispatch:

jobs:
  apply:
    uses: naxa-developers/gh-workflows/.github/workflows/apply.yml@tm
    with:
      working_dir: ./scripts/aws/infra/dev/
      terraform_version: "1.9.5"
      terragrunt_version: "0.67.15"
      aws_region: us-west-2
    secrets: inherit
