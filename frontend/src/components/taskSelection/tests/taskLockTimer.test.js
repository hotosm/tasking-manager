import {} from '';

describe('', () => {
  const activeTasks = [
    {
      taskId: 84,
      projectId: 1,
      taskStatus: 'LOCKED_FOR_MAPPING',
      lockHolder: 'user1',
      taskHistory: [],
      taskAnnotation: null,
      isUndoable: false,
      autoUnlockSeconds: 7200,
      lastUpdated: new Date(),
    },
  ];
  it('', () => {
    expect(true).toBe(true);
  });
});
