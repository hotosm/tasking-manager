(function () {

    'use strict';

    /**
     * Dashboard controller which manages the application page
     */
    angular
        .module('taskingManager')
        .controller('applicationKeyController', []);

    function applicationKeyController() {

    }
})();
